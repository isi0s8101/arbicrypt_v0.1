import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    // Définir l'état initial de l'application
  },
  mutations: {
    // Définir les mutations pour modifier l'état
  },
  actions: {
    // Définir les actions pour effectuer des opérations asynchrones
  },
  modules: {
    // Définir des modules pour diviser le store en morceaux
  }
})
